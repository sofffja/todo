task
  -propiedades
  -toggleDone
  -setPriority

list
  -addTask
  -removeTask
  -getTasks

listsArray
  -addList
  -removeList
  -setCurrent
  -getCurrent
  -getLists

renderListsArray
  for list of listsArray.getLists
    titulo
      eventListener ->
        listsArray.setCurrent
        renderListsArray
    removeBtn
      eventListener ->
        listsArray.removeList
        renderListsArray

renderTasks
  for task of listsArray.getCurrent.getTasks
    task.propiedades
    doneBtn
      eventListener ->
        toggleDone
        toggle class?
    removeBtn
      eventListener ->
        listsArray.getCurrent.removeTask
        renderTasks

DOMHandler
  DOMInit
    si hay localStorage -> loadLocalStorage
    sino -> loadDefault
  loadDefault
  loadLocalStorage
  modalForm



FIX
-que hacer con la descipcion
  decia que puedas desplegar para ver mas info capazzz
-poner bien las fechas
-si task completo que quede tachado o que directamente se borre?
-mas bonito, hovers, etc
